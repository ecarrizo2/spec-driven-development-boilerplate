---
id: 1
epic_id: 1
title: "Validate API dispatch and execution PR creation"
status: refined
repo: "sdd-test-api"
depends_on: []
complexity: 2
jira_ticket: null
---

# Request: Validate API dispatch and execution PR creation

## Goal

Verify that merging the approved plan for task 1 dispatches correctly to `sdd-test-api` and opens an execution branch and draft PR in the child repository.

## Requirements

- Dispatch payload includes valid task metadata, hub reference, and integrity token.
- Child repo reusable receive workflow accepts payload and creates execution branch.
- Child repo opens draft execution PR with hub cross-reference fields.

## Acceptance Criteria

- WHEN plan PR for task 1 is merged, the system SHALL dispatch `task-assigned` to `ecarrizo2/sdd-test-api`.
- WHEN the child repo receives the dispatch, the system SHALL create the execution branch and draft PR.
- IF dispatch payload is invalid or tampered, the system SHALL fail the handoff and report workflow failure.

<!-- ai-plan:start -->
## AI Plan Synthesis
**Summary:** Implement the validation of API dispatch and execution PR creation for task 1 in the sdd-test-api repository as part of the multi-repo AI workflow smoke test.
### Implementation Steps
- Review the epic and task graph to confirm dependency order and task requirements.
- Ensure the request file for task 1 exists in the sdd-test-api repository and matches the expected naming convention.
- Update the epic-create-plans.yml workflow to include logic for creating a plan PR for task 1 with the correct metadata.
- Verify the plan-agent-pickup.yml workflow synthesizes the plan content and publishes the 'Plan Generated by Agent' check.
- Implement dispatch logic in the plan-execution-trigger.yml workflow to send a 'task-assigned' payload to the sdd-test-api repository upon merging the task 1 plan PR.
- Ensure the reusable receive-task.yml workflow in the sdd-test-api repository processes the dispatch payload correctly, creates an execution branch, and opens a draft execution PR.
- Add validation for the dispatch payload to ensure it includes valid task metadata, hub reference, and integrity token.
- Update delivery tracking metadata to include the execution PR URL for task 1.
### Files to Touch
- .github/workflows/epic-create-plans.yml
- .github/workflows/plan-agent-pickup.yml
- .github/workflows/plan-execution-trigger.yml
- sdd-test-api/.github/workflows/receive-task.yml
- sdd-test-api/request/task-1.yml
### Verification
- Approve and merge the epic PR to trigger plan PR creation for task 1.
- Confirm the plan PR for task 1 is created with the correct metadata and linked to the epic.
- Verify the 'Plan Generated by Agent' check passes successfully for the task 1 plan PR.
- Merge the task 1 plan PR and confirm the dispatch payload is sent to the sdd-test-api repository.
- Verify the sdd-test-api repository processes the dispatch payload and creates the execution branch and draft PR.
- Check the draft execution PR in the sdd-test-api repository for correct hub cross-reference fields and metadata.
- Ensure delivery tracking metadata is updated with the execution PR URL for task 1.
### Risks
- Dispatch payload may fail validation due to missing or incorrect metadata.
- Reusable workflows in the sdd-test-api repository may not correctly process the dispatch payload.
- Errors in the epic-create-plans.yml or plan-execution-trigger.yml workflows could prevent plan PR creation or dispatch.
- Dependency order enforcement may fail if task graph metadata is inconsistent.
### Open Questions
- Are there additional validation checks required for the dispatch payload?
- Should the draft execution PR include specific labels or reviewers by default?
- What is the expected behavior if the dispatch payload fails validation in the sdd-test-api repository?
<!-- ai-plan:end -->