name: "Sync to Hub"

on:
  pull_request:
    types: [closed]

jobs:
  sync-hub:
    if: >
      github.event.pull_request.merged == true &&
      contains(github.event.pull_request.labels.*.name, 'sdd-execute')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Update hub delivery manifest
        uses: actions/github-script@v7
        env:
          HUB_REPO: ${{ vars.HUB_REPO }}
          HUB_TOKEN: ${{ secrets.HUB_TOKEN }}
        with:
          github-token: ${{ secrets.HUB_TOKEN }}
          script: |
            const [hubOwner, hubRepo] = process.env.HUB_REPO.split('/');
            const prNumber = context.payload.pull_request.number;
            const prUrl = context.payload.pull_request.html_url;
            const repo = context.repo.repo;

            // 1. Read delivery.yaml from hub main
            // 2. Find the node matching this repo + PR
            // 3. Update status to 'merged', set pr_url and pr_number
            // 4. Commit the update to hub main (or open a PR)
            // 5. Update task-graph.md similarly

            console.log(`Syncing PR #${prNumber} (${prUrl}) from ${repo} to hub ${process.env.HUB_REPO}`);

            // TODO: Implement full delivery.yaml update via GitHub Contents API
            // This requires reading the file, parsing YAML, updating the node,
            // and committing the change back to the hub repo.

            core.info(`Hub sync placeholder — configure HUB_REPO and HUB_TOKEN to enable`);
